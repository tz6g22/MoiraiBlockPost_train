seed: 42
base_checkpoint: outputs/base/qwen3_14b_full_attnres
data_manifest: outputs/data/splits.json
data_config: configs/data.yaml
partition_root: outputs/formal/discovery
query_root: outputs/formal/adapter
probe_entry_task: math
classes:
  0: math
  1: multihop
  2: code
confidence_threshold: 0.5
feature_site: transformer_block_0_output
pooling: mask_aware_mean
classifier: "Linear(5120,3)"
feature_batch_size: 1
batch_size: 256
max_epochs: 20
learning_rate: 1.0e-3
weight_decay: 1.0e-4
output_dir: outputs/formal/probe
